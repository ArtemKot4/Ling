# Операторы 🕛:

Операторы являются основой программирования локализации внутри ling файлов.

## Математические:

Позволяют производить математические операции внутри функций и выражений.

Математические выражения возможны с функциями, строками, числами и переводами, при обращении по пакету.

```ling
package example {
    //плюс
    a = 1 + 1 
    //умножение
    b = 2 * 2 
    //деление
    c = 12 / 2
    //возведение в степень
    d = 4 ** 2
    //минус
    e = 10 - 5

    hello = "hello" * 2 //hellohello
    table = "Table"
    table_count = table + " x " + 10_000 //Table x 10000
}
```

## Логические:

```lings
package example {
    a = 10
    b = 20

    //равенство
    c = a == b
    //логическое "и"
    d = a and b 
    //логическое "или"
    e = a or b 
    //неравенство
    f = a != b
    //отрицание
    g = not b 
    //больше
    h = a > b
    //меньше
    i = a < b
    //больше или равно
    j = a >= b
    //меньше или равно
    k = a <= b
    //да
    l = true
    //нет
    m = false
}
```

## Индексы:

Индексы позволяют более эффективно взаимодействовать со строками, не прибегая к функциям для перестановки значений. Чаще всего используются с входящими аргументами.

Выражение с индексом возвращает итоговую строку либо число, взятую из индекса.

Синтаксис такой: `"строка"[индекс_от,индекс_до,шаг]`. 

Если мы пропускаем индексы в начале, то вместо запятой ставим точку.

Например `"hello"[1]` вернёт `"e"`, в то время как `"hello"[.2]` вернёт `"he"`.

```ling
package example {
    reverse(a, b = 10) {
        a[..-1] * b
    }
    reverse_implementation = reverse("hello", 2) //ellohelloh
    point = "hello"[3] //lo
    all = "hello"[.3, 2] //hl
}
```