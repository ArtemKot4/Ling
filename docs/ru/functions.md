# Функции 🕛:

Функции представляют собой шаблоны под переводы, которые могут принимать значения и использовать их. Вы можете объявить их в пакетах, в другом случае они будут помещены в пакет `common`.

Чтобы вернуть значение не нужны операторы, достаточно записать его в фигурных скобках. Значением является результат последнего вычисленного выражения.

Функция обязана возвращать значение, иначе будет ошибка.

Вы не можете объявить одну и ту же функцию под одну и ту же локализацию повторно.

Содержание:

1. [Объявление](#объявление☑:)
2. [Вызов функций](#вызов-функций)
3. [Аргументы по-умолчанию](#аргументы-по-умолчанию-☑)
4. [Условия](#условия)
5. [Перегрузки под локализации](#перегрузки-под-локализации-☑)
6. [Декораторы аргументов](#декораторы-аргументов)
7. [Советы](#советы)

## Объявление ☑:

Для объявления функции достаточно дать ей название и открыть круглую скобку.

```ling
package welcome {
    hello(user) {
        "Hello, ${user}!"
    }
}
```

#### Модификатор override ☑:

В случае, если вы хотите переписать имеющуюся функцию, вы можете использовать модификатор override, тогда ошибки не будет:

```ling
example() {
    "example original"
}


override example() {
    "example overrided"
}
```



## Вызов функций:

Для вызова функций нужно написать название функции, открыть скобку и передать значения.

```ling
package test {
    hello = welcome.hello("Artem") //Hello, Artem!
}
```

Так же вы можете передавать аргументы с явным указанием их названия. В таком случае эти аргументы не будут занимать позицию и сразу же передадут значение. Попытка передать значение в несуществующий аргумент вызовет ошибку.

```linga
package test {
    hello = welcome.hello(user="Artem") //Hello, Artem!
}
```

## Аргументы по-умолчанию ☑:

Функции могут принимать в аргументы значения по-умолчанию, в выражениях которых могут использоваться значения других аргументов.

Создадим функцию для умножения аргументов на 3, либо переданное число в multiply

```ling
package math {
    multiply_by(a, b, miltiply = 3) {
        "${a * 3}, ${b * 3}"
    }
}
```

## Условия:

Функции могут иметь условия внутри себя, что позволяет реализовать интерполяцию, склонение, сокращение и другие вещи, полезные в локализации. 

Условий может быть неограниченное количество, они выполняются сверху-вниз. После того, как условие выполнилось, дальнейшие условия не будут просмотрены, вернётся выражение из блока и станет возвращаемым типом.

Условия могут вкладываться друг в друга.

Создадим функцию для сокращения чисел, которые больше тысячи, в вид `первая_цифра + К`

```ling
package short {
    number(n) {
        match(n > 1000) {
            n[0] + "К" //значение, которое вернётся при выполнении условия
        }
        n //значение по-умолчанию
    }
}
```

## Перегрузки под локализации ☑:

Случаются ситуации, в которых в зависимости от языка нужны совсем разные правила обработки. В таком случае нам нужно создать функцию с одним и тем же названием, но для конкретных языков.

Если указанный язык не определён глобально или в пакете, вы получите ошибку.

Объявление таких функций выглядит так: `название_функции:название_языка_локализации`

```ling
define langs = en-US, ru-RU

package ui {
    // Общая функция, сработает для любого языка, для которого не переопределена отдельно
    users_online(count) {
        count + " users online"
    }

    // Сработает для русского языка
    users_online:ru-RU(count) {
        match count % 10 == 1 and count % 100 != 11: count + " пользователь онлайн"
        match count % 10 >= 2 and count % 10 <= 4 and not (count % 100 >= 12 and count % 100 <= 14): count + " пользователя онлайн"
        count + " пользователей онлайн"
    }
}
```

То же самое работает и для `unexpected`.

## Декораторы аргументов

Вы можете применять преобразования для аргументов сразу же при их поступлении глобально.

#### Объявление декоратора аргументов

Декораторы аргументов являются самыми обычными функциями, но они не могут иметь больше одного аргумента. Иначе он сможет использоваться только как обычная функция.

Создадим декоратор аргументов:

```ling
package messages {
    hi(name) {
        "Hi, ${name}!"
    }
}
```

#### Используем

Сейчас мы рассмотрим два примера, и первый будет совпадать со вторым по возвращаемому значению, но первый будет использовать декораторы аргументов.

1.
    ```ling
    nice_message(@messages.hi user) {
        user + " Have a nice day!"
    } 
    ```
2.
    ```ling
    nice_message(user) {
        messages.hi(user) + " Have a nice day!"
    } 
    ```

Создадим перевод:

```ling
sayhi_artem = nice_message("Artem") //Hi, Artem! Have a nice day!
```

## Советы

1. Старайтесь делать так, чтобы
   сигнатура перегрузок под разные языки совпадала с сигнатурой основной функции. 
   Если это не так, ошибку вы не получите, но предупреждение в консоли будет ☑.

2. Называйте функции только в одном стиле, но желательно в camel case.
