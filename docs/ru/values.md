# Значения
Здесь описаны значения, которыми могут быть функции и переводы и которые могут использоваться в выражениях.

Содержание:
1. [Строки](#строки-☑)
2. [Регулярные выражения](#регулярные-выражения-☑)
3. [Числа](#числа)

## Строки ☑:

Строки позволяют переводам и выражениям хранить слова.

#### Обычные строки

Обычные строки объявляются при помощи одинарных `'` и двойных `"` кавычек.

```ling
example_translation_one = 'one'
example_translation_two = "two"
```

Они ничем друг от друга не отличаются.
Такие строки не будут при переносе строк содержать в себе символы переносов строк: `\n`, `\r`

```ling
example_some_lines = "I love orange.
Orange is one of my favorite colors in paintings
"
```
В итоге такая строка вернёт текст вида: `I love orange.Orange is one of my favorite colors in paintings`

#### Строки с сохранением переносов строк

Такие строки объявляются при помощи угловых кавычек и обычно используются только для сохранения символов переносов.

```ling
package mod.infinite_forest {
    info = `Infinite Forest 
    est silva in lux,
    non locus in malum.`
    //отступы обрабатываются, пробелы в начале будут добавлены только, если их будет больше чем у info
}
```

В итоге такая строка вернёт текст вида: `Infinite Forest\nest silva in lux,\nnon locus in malum.`

### Поддержка выражений:

Строки поддерживают выражения внутри себя, точно так же, как и в JavaScript. 

```ling
define langs = en-US, ru-RU

package words {
    "and" = "and", "и"
}

package home {
    table = "table", "стол"
    chair = "chair", "стул"
    together = "${table + " " + words.and + " " + chair}" //table and chair в одной локализации, стол и стул в другой
}
```

Давайте рассмотрим пример с математикой:

```ling
define langs = en-US

package math {
    numbers = "5 + 5 is ${5 + 5}, 5 * 5 is ${5 * 5}" //5 + 5 is 10, 5 * 5 is 25
}
```

После компиляции это выражение становится таким:

 `"5 + 5 is " + (5 + 5) + ", 5 * 5 is " + (5 * 5)`

Важно! В строках внутри выражений, выражений быть не может. Они будут проигнорированы и добавятся в строку как текст.

Также выражения внутри строк не могут быть пустыми, это приведёт к ошибке.

В случае экранирования `\${`, выражение создано не будет.

#### Математические выражения 🕛:
Вы можете умножать и складывать строки:
```ling
"hello" * 2 + "!" //hellohello!
```

## Регулярные выражения ☑:

Регулярные выражения тут точно такие же, как и в JavaScript. 

Создаются при помощи  следующего синтаксиса: `/выражение/`.

После закрывающего символа вы можете перечислить любые существующие флаги в любом количестве. При этом при попытке добавить несуществующий флаг возникнет соответствующая ошибка.

Вы можете дублировать флаги, выражения по типу `/[a-z]/igg` не вызовут ошибку

Давайте рассмотрим пример, который позволит проверить, является ли переданный аргумент числом:

```math.ling
package math {
    is_number(a) {
        a in /^\d+$/
    }
}
```

В данном случае оператор `in` является заменой `new RegExp("^\d+$").test(a)` и возвращает логическое `true` или `false`. Это позволяет более понятно и просто создавать такие проверки и пригодится в случае, если вам нужно отобразить какую-то информацию пользователю, при этом вне клиента она не важна. 

Это пригодится не только для локализации, ведь язык позволяет описывать функции обработки и для любых других задач форматирования, что особенно удобно при генерации кода в JavaScript.

При каждом вызове функции создаётся новое RegExp, в которое помещается наше регулярное выражение и флаги.

<!---
#### Разрешение идентификаторов

В случае, если внутри регулярного выражения будут идентификаторы, а именно названия аргументов или переводов, они могут встроиться внутрь регулярного выражения.

Используйте `${char}` подстановки. Вы уже привыкли использовать их внутри переводов, и тут ситуация очень похожая, но ссылается прямиком на идентификатор
-->
## Числа ☑:
Числа помогают в расчётах и используются преимущественно в функциях. Могут быть дробные и целые.

```ling
//просто число
10000 
//число с нижним подчёркиванием
10_000 
//дробное число
10000.0 
```