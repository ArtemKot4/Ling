# Спецификация

Здесь описан синтаксис и семантика языка ling. 

**Статус реализации:** ✔ реализовано, 🕛 в разработке.

## Определения ✔:

Определения позволяют сделать возможной поддержку некоторых настроек. Могут быть заданы в любой строке файла. Все настройки являются опциональными.

```ling
define {
    langs = en-US //языки в формате lowercase-UPPERCASE через запятую
    encoding = "utf-8"
    unexpected(key) {
        "Ключ " + key + " не найден!"
    }
}
```

Или более сокращённая форма под конкретный параметр:

```ling
//define название идентификатор_начала значение

define langs = en-US
define encoding = "utf-8"
define unexpected(key) {
    "Несуществующий ключ: " + key
}
```

Определений может быть неограниченное количество. Языки из определений складываются, в то время как другие параметры перезаписываются. Языки не должны повторяться.

## Пакеты ✔:

Пакеты позволяют запаковывать переводы в нечто похожее на объекты json. Создаются при помощи ключевых слов `package`.

```ling
define langs = ru-RU, en-US

package navbar {
    package faq {
        about_us = "О нас", "About us"
    }
}

//или 

package navbar.faq {
    idea = "Идея", "Idea"
}


//перевод, выдаваемый в случае попытки получить незарегистрированный перевод в пакете

package navbar.faq {
    unexpected(key) {
        "Ключ " + key + " не найден в navbar.faq"
    }
}
```



## Строки ✔

Строки как раз и позволяют переводам хранить слова. Создаются только при помощи двойных кавычек. Могут быть в одну строку или в несколько.

Синтаксис такой: `"любой текст"`

```ling
define langs = en-US

package descriptions {
    //в одну строку
    glowworms = "I love glowworms. They so beautiful, and, seems, they not afraid of dark. I love them, and, seems, they love me." 

    //в несколько строк. Все отступы учитываются
    forest = "
        Infinite Forest 
        est silva in lux,
        non locus in malum.
    "
}

```

#### Поддержка выражений

Переводы также поддерживают выражения внутри себя, точно так же, как и в JavaScript. 

```ling
define langs = en-US, ru-RU

package words {
    and = "and", "и"
}

package home {
    table = "table", "стол"
    chair = "chair", "стул"
    together = "${table + " " + words.and + " " + chair}" //table and chair в одной локализации, стол и стул в другой
}
```

Давайте рассмотрим пример с математикой

```ling
define langs = en-US

package math {
    numbers = "5 + 5 is ${5 + 5}, 5 * 5 is ${5 * 5}" //5 + 5 is 10, 5 * 5 is 25
}
```

После компиляции это выражение становится таким:

 `"5 + 5 is " + (5 + 5) + ", 5 * 5 is " + (5 * 5)`

## Переводы 🕛:

Переводы помещаются в пакеты и могут быть заданы в одну строку или в несколько.

#### В одну строку:

```ling
define langs = ru-RU, en-US

package navbar.settings { 
    theme = "Тема", "Theme"
}
```

#### В несколько строк:

```ling
define langs = ru-RU, en-US

package navbar.settings {
    color {
        "цвет",
        "color"
    }
}
```

## Комментарии ✔:

Комментарии позволяют делать заметки в коде и ни на что не влияют.

#### Однострочные:

```ling
//комментарий
```

#### Многострочные:

```ling
/* комментарий */
```

## Операторы 🕛:

Операторы являются основой программирования локализации внутри ling файлов.

#### Математические:

Позволяют производить математические операции внутри функций и выражений.

Математические выражения возможны с функциями, строками, числами и переводами, при обращении по пакету.

```ling
package example {
    //плюс
    a = 1 + 1 
    //умножение
    b = 2 * 2 
    //деление
    c = 12 / 2
    //возведение в степень
    d = 4 ** 2
    //минус
    e = 10 - 5

    hello = "hello" * 2 //hellohello
    table = "Table"
    table_count = table + " x " + 10_000 //Table x 10000
}
```

#### Логические:

```lings
package example {
    a = 10
    b = 20

    //равенство
    c = a == b
    //логическое "и"
    d = a and b 
    //логическое "или"
    e = a or b 
    //неравенство
    f = a != b
    //отрицание
    g = not b 
    //больше
    h = a > b
    //меньше
    i = a < b
    //больше или равно
    j = a >= b
    //меньше или равно
    k = a <= b
    //да
    l = true
    //нет
    m = false
}
```

#### Технические

Оператор `?` позволяет указать транспилятору, что вместо ошибки нужно подставлять значение null в значения локализации перевода, которые не указаны.

Этот оператор создан скорее для быстрой разработки, в случае если вы не успеваете доделать все необходимые переводы. После публикации или обновления проекта лучше его не использовать, если это поведение не является запланированным.

Например:

```ling
define langs = en-US, ru-RU

package home {
    main = "Main room" //Ошибка: ожидался перевод для ru-RU
    //или
    main? = "Main room" //Значение для ru-RU стало null
    //или
    main = "Main room", "Главная комната" //Идеальный вариант, всех переводов хватает, ошибок и null значений нет
}
```

### Индексы 🕛:

Позволяют более эффективно взаимодействовать со строками, не прибегая к функциям для перестановки значений. Чаще всего используются с входящими аргументами.

Выражение с индексом возвращает итоговую строку либо число, взятую из индекса.

Синтаксис такой: `"строка"[индекс_от,индекс_до,шаг]`. 

Если мы пропускаем индексы в начале, то вместо запятой ставим точку.

Например `"hello"[1]` вернёт `"e"`, в то время как `"hello"[.2]` вернёт `"he"`.

```ling
package example {
    reverse(a, b = 10) {
        a[..-1] * b
    }
    reverse_implementation = reverse("hello", 2) //ellohelloh
    point = "hello"[3] //lo
    all = "hello"[.3, 2] //hl
}
```

### Функции 🕛:

Функции представляют собой переводы, которые могут принимать значения и использовать их. 

Чтобы вернуть значение не нужны операторы, достаточно записать его в фигурных скобках. Значением является результат последнего вычисленного выражения.

Для объявления функции достаточно дать ей название и открыть круглую скобку.

#### Аргументы по-умолчанию ✔:

Функции могут принимать в аргументы значения по-умолчанию, в выражениях которых могут использоваться значения других аргументов.

Создадим функцию для умножения аргументов на 3, либо переданное число в multiply

```ling
package math {
    multiply_3(a, b, miltiply = 3) {
        "${a * 3}, ${b * 3}"
    }
}
```

#### Условия🕛

Функции могут иметь условия внутри себя, что позволяет реализовать интерполяцию, склонение, сокращение и другие вещи, полезные в локализации. 

Условий может быть неограниченное количество, они выполняются сверху-вниз. После того, как условие выполнилось, дальнейшие условия не будут просмотрены, вернётся выражение после двоеточия и станет возвращаемым типом.



Создадим функцию для сокращения чисел, которые больше тысячи, в вид `первая_цифра + К`

```ling
package short {
    number(n) {
        match n > 1000: n[0] + "К" //значение, которое вернётся при выполнении условия
        n //значение по-умолчанию
    }
}
```

#### Переопределение функции под разные языки локализации ✔

Случаются ситуации, в которых в зависимости от языка нужны совсем разные правила обработки. В таком случае нам нужно создать функцию с одним и тем же названием, но для конкретных языков.

Объявление таких функций выглядит так: `название_функции:название_языка_локализации`

```ling
define langs = en-US, ru-RU

package ui {
    // Общая функция, сработает для любого языка, для которого не переопределена отдельно
    users_online(count) {
        count + " users online"
    }

    // Сработает для русского языка
    users_online:ru-RU(count) {
        match count % 10 == 1 and count % 100 != 11: count + " пользователь онлайн"
        match count % 10 >= 2 and count % 10 <= 4 and not (count % 100 >= 12 and count % 100 <= 14): count + " пользователя онлайн"
        count + " пользователей онлайн"
    }
}
```

То же самое работает и с `unexpected`.

#### Документирование функций

Эта особенность будет работать в ling файлах, но так же и в JavaScript, но только в варианте с генерацией кода, так как TypeScript не позволяет создавать динамические комментарии к типам.

```ling
define langs = en-US, ru-RU

package home {
    hi = "Hi", "Привет"
    /**
     * Function that returns text in format `Hi, %name!`
     */
    get_hi(name) {
        "${hi}, ${name}!"
    }
}
```
